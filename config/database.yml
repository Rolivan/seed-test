defaults: &defaults
  adapter: mysql2
  database: seedtest
  host: localhost
  encoding: utf8
  username: root
  password: root

development:
  <<: *defaults
  database: seed_test_development

test:
  <<: *defaults
  database: seed_test_test

production:
  <<: *defaults
  database: seed_test